const configs = {
  rooms: [
    "livingroom",
    "bathroom",
    "bedroom1",
    "bedroom2",
    "terrace",
    "carport",
  ],
  url: `ws://${window.location.hostname}/ws`,
};

export default configs;
