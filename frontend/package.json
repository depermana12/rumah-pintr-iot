{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite --port 3000",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -r ../data",
    "clean-build": "bun run clean && bun run build"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  }
}
